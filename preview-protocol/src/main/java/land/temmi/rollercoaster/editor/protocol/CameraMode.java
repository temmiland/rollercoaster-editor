package land.temmi.rollercoaster.editor.protocol;

/** How the preview positions its camera. */
public enum CameraMode {
    /** Mouse-orbit inspection camera, for authoring. */
    FREE,
    /** The same fixed-pitch, subject-framed camera the game itself uses. */
    GAME
}
