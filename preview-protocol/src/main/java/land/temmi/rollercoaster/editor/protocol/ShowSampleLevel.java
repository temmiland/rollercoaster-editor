package land.temmi.rollercoaster.editor.protocol;

/**
 * Editor sends this when a project is open. Stands in for transferring an actual document
 * (Phase 2/3) - for now the preview just shows its bundled sample level.
 */
public final class ShowSampleLevel {
}
